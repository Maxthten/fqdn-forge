id: 101-proxy-target-canonicalization
name: Proxy target canonicalization
description: Positive local reference scenario for exact numeric-loopback proxy allowlisting.
version: "1.4.0"
root_domain: s101.v14.test
seed: 101
include_root: false
allow_duplicates: false
allow_concurrent: true
coverage_tags: { source_shape: [generic], payload_format: [json], pagination: [none], authentication: [proxy_auth], network_profile: [http_proxy], quota_scope: [none], transport: [identity], fault_class: [egress], execution_style: [single], assertion_focus: [audit, isolation, redaction] }
composition: { fault_stages: [proxy_connection], event_order: [{ before: external_target_rejected, after: next_valid_request }] }
endpoints:
  - id: source
    source_kind: generic_json
    match: { method: GET, path: /v14/101, query: { domain: { equals: $TARGET_DOMAIN } }, headers: { x-lab-run-id: { present: true } } }
    extract: { items_field: items, candidate_field: host, record_id_field: id }
    replies: [{ status: 200, body: { items: [{ id: s101, host: api.s101.v14.test }] } }]
