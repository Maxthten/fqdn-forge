id: 112-concurrent-mixed-fault-soak
name: Concurrent mixed fault soak
description: Fixed local scenario anchoring concurrent deterministic soak actions.
version: "1.4.0"
root_domain: s112.v14.test
seed: 112
include_root: false
allow_duplicates: false
allow_concurrent: true
coverage_tags: { source_shape: [generic], payload_format: [json], pagination: [none], authentication: [proxy_auth], network_profile: [http_proxy], quota_scope: [per_source], transport: [chunked], fault_class: [lifecycle], execution_style: [soak], assertion_focus: [audit, isolation, resource, determinism] }
composition: { fault_stages: [proxy_connection, source_request, lifecycle], event_order: [{ before: concurrent_run, after: resource_zero }] }
endpoints:
  - id: source
    source_kind: generic_json
    match: { method: GET, path: /v14/112, query: { domain: { equals: $TARGET_DOMAIN } }, headers: { x-lab-run-id: { present: true } } }
    extract: { items_field: items, candidate_field: host, record_id_field: id }
    replies: [{ status: 200, body: { items: [{ id: s112, host: api.s112.v14.test }] } }]
