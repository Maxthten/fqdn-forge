id: 114-coverage-and-baseline-integrity
name: Coverage and baseline integrity
description: Fixed scenario for computed coverage metadata and logical baseline provenance.
version: "1.4.1"
root_domain: s114.v14.test
seed: 114
include_root: false
allow_duplicates: false
allow_concurrent: true
coverage_tags: { source_shape: [generic], payload_format: [json], pagination: [none], authentication: [none], network_profile: [direct], quota_scope: [none], transport: [identity], fault_class: [none], execution_style: [replay], assertion_focus: [audit, determinism, resource, isolation] }
composition: { fault_stages: [lifecycle], event_order: [{ before: coverage_generated, after: baseline_compared }] }
endpoints:
  - id: source
    source_kind: generic_json
    match: { method: GET, path: /v14/114, query: { domain: { equals: $TARGET_DOMAIN } }, headers: { x-lab-run-id: { present: true } } }
    extract: { items_field: items, candidate_field: host, record_id_field: id }
    replies: [{ status: 200, body: { items: [{ id: s114, host: api.s114.v14.test }] } }]
