id: 094-proxy-auth-then-source-rate-limit
name: Proxy authentication then source rate limit
description: Local compositional regression record for proxy, source and quota ordering.
version: "1.4.1"
root_domain: s094.v14.test
seed: 94
runner: { max_retries: 2, retry_after_cap_ms: 2000 }
network_profile: { mode: http_proxy, proxy_must_be_used: true, allow_retry: true, initial_proxy_auth_challenge: true }
include_root: false
allow_duplicates: false
allow_concurrent: true
coverage_tags: { source_shape: [generic], payload_format: [json], pagination: [none], authentication: [proxy_auth], network_profile: [http_proxy], quota_scope: [per_source], transport: [identity], fault_class: [rate_limit], execution_style: [single], assertion_focus: [finding, audit, redaction] }
composition: { fault_stages: [proxy_connection, source_request, retry_recovery], event_order: [{ before: proxy_authenticated, after: source_request }] }
fault_script:
  - { id: proxy-authenticated, stage: proxy, endpoint: source }
  - { id: source-rate-limit, stage: source, endpoint: source, expect_quota_rate_limited: true }
  - { id: source-retry-success, stage: source, endpoint: source, response_index: 0, minimum_virtual_wait_ms: 2000 }
endpoints:
  - id: source
    source_kind: generic_json
    match: { method: GET, path: /v14/094, query: { domain: { equals: $TARGET_DOMAIN } }, headers: { x-lab-run-id: { present: true } } }
    extract: { items_field: items, candidate_field: host, record_id_field: id }
    quota: [{ scope: per_source, success_limit: 0, exhausted_status: 429, retry_after_mode: seconds, retry_after_ms: 2000, recover_after_virtual_ms: 2000 }]
    replies: [{ status: 200, body: { items: [{ id: s094, host: api.s094.v14.test }] } }]
