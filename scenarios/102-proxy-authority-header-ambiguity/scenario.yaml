id: 102-proxy-authority-header-ambiguity
name: Proxy authority header ambiguity
description: Static regression record for absolute-form and authority-header disagreement.
version: "1.4.1"
root_domain: s102.v14.test
seed: 102
include_root: false
allow_duplicates: false
allow_concurrent: true
coverage_tags: { source_shape: [generic], payload_format: [json], pagination: [none], authentication: [proxy_auth], network_profile: [connect_proxy], quota_scope: [none], transport: [identity], fault_class: [egress], execution_style: [single], assertion_focus: [audit, isolation, redaction] }
composition: { fault_stages: [proxy_connection], event_order: [{ before: authority_checked, after: source_not_reached }] }
network_profile: { mode: connect_proxy, proxy_must_be_used: true }
endpoints:
  - id: source
    source_kind: generic_json
    match: { method: GET, path: /v14/102, query: { domain: { equals: $TARGET_DOMAIN } }, headers: { x-lab-run-id: { present: true } } }
    extract: { items_field: items, candidate_field: host, record_id_field: id }
    replies: [{ status: 200, body: { items: [{ id: s102, host: api.s102.v14.test }] } }]
