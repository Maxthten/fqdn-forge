id: 113-replay-provenance-and-multi-diff
name: Replay provenance and multi-diff
description: Fixed scenario for digest-aware strict replay and bounded multi-difference diagnostics.
version: "1.4.1"
root_domain: s113.v14.test
seed: 113
include_root: false
allow_duplicates: false
allow_concurrent: true
coverage_tags: { source_shape: [generic], payload_format: [json], pagination: [none], authentication: [none], network_profile: [direct], quota_scope: [none], transport: [identity], fault_class: [lifecycle], execution_style: [replay], assertion_focus: [finding, audit, determinism, resource] }
composition: { fault_stages: [lifecycle], event_order: [{ before: provenance_checked, after: semantic_diff_classified }] }
endpoints:
  - id: source
    source_kind: generic_json
    match: { method: GET, path: /v14/113, query: { domain: { equals: $TARGET_DOMAIN } }, headers: { x-lab-run-id: { present: true } } }
    extract: { items_field: items, candidate_field: host, record_id_field: id }
    replies: [{ status: 200, body: { items: [{ id: s113, host: api.s113.v14.test }] } }]
