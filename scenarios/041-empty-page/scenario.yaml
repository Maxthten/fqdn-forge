id: 041-empty-page
name: Empty page termination
description: An empty successful cursor page terminates without an extra request.
version: "1.2"
coverage_tags:
  assertion_focus: [finding]
  authentication: [none]
  execution_style: [concurrent]
  fault_class: [none]
  network_profile: [direct]
  pagination: [cursor]
  payload_format: [json]
  quota_scope: [none]
  source_shape: [generic]
  transport: [identity]
allow_duplicates: false
allow_concurrent: true
root_domain: acme.test
seed: 41
endpoints:
  - id: empty-page
    source_kind: generic_json
    match: { method: GET, path: /v12/empty-page, query: { domain: { equals: $ROOT_DOMAIN } } }
    pagination: { mode: cursor, parameter: cursor, next_cursor_field: next }
    extract: { format: json, items_field: items, candidate_field: host }
    replies: [{ status: 200, body_file: fixtures/data.json }]
