id: 097-source-503-then-chunked-success
name: Source 503 then chunked success
description: Deterministic chunked local payload for bounded retry and transport coverage.
version: "1.4.0"
root_domain: s097.v14.test
seed: 97
include_root: false
allow_duplicates: false
allow_concurrent: true
coverage_tags: { source_shape: [generic], payload_format: [json], pagination: [none], authentication: [none], network_profile: [direct], quota_scope: [none], transport: [chunked], fault_class: [upstream], execution_style: [single], assertion_focus: [finding, audit, determinism] }
composition: { fault_stages: [source_request, response_transport, retry_recovery], event_order: [{ before: upstream_failure, after: chunked_success }] }
endpoints:
  - id: source
    source_kind: generic_json
    match: { method: GET, path: /v14/097, query: { domain: { equals: $TARGET_DOMAIN } }, headers: { x-lab-run-id: { present: true } } }
    extract: { items_field: items, candidate_field: host, record_id_field: id }
    replies: [{ status: 200, transfer_mode: chunked, chunk_count: 3, body: { items: [{ id: s097, host: api.s097.v14.test }] } }]
