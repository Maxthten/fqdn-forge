id: 105-stale-capability-after-reset-delete
name: Stale capability after reset delete
description: Lifecycle regression for revoked run and proxy capabilities.
version: "1.4.1"
root_domain: s105.v14.test
seed: 105
include_root: false
allow_duplicates: false
allow_concurrent: true
coverage_tags: { source_shape: [generic], payload_format: [json], pagination: [none], authentication: [expired], network_profile: [http_proxy], quota_scope: [none], transport: [identity], fault_class: [lifecycle], execution_style: [concurrent], assertion_focus: [audit, isolation, determinism] }
network_profile: { mode: http_proxy, proxy_must_be_used: true }
composition: { fault_stages: [lifecycle], event_order: [{ before: capability_revoked, after: old_capability_rejected }] }
endpoints:
  - id: source
    source_kind: generic_json
    match: { method: GET, path: /v14/105, query: { domain: { equals: $TARGET_DOMAIN } }, headers: { x-lab-run-id: { present: true } } }
    extract: { items_field: items, candidate_field: host, record_id_field: id }
    replies: [{ status: 200, body: { items: [{ id: s105, host: api.s105.v14.test }] } }]
